# HTML, CSS, JS, Git intro

## Git 
 "доллар в начале печатаь не нужнол"
1. Chek version: 

    ```bash
    $ git --version
    $ ls -la
    $ rm -rf .git/
    ```

2.  Git init a new repository

`$ git init`

